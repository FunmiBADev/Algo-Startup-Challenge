run = "npm run replit-start:full"
language = "nodejs"
hidden = ["node_modules", "dist", "backend/node_modules"]

[nix]
channel = "stable-23_11"

[env]
# Replit provides PORT automatically; this is a fallback
PORT = "3000"

